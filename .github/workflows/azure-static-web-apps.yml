# Disabled duplicate workflow — deploy uses azure-static-web-apps-agreeable-forest-0c76fb500.yml
# (this file's token secret was missing and caused false failures)
name: Azure Static Web Apps CI/CD (legacy disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use azure-static-web-apps-agreeable-forest-0c76fb500.yml"
